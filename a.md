Question 1
A web developer is troubleshooting an HTTP response with a status code of 404. What does this indicate about the client’s request?


The server encountered an internal error while processing the request 

The requested resource could not be found on the server 

The request was successful, and the resource was retrieved 

The client lacks proper authentication to access the resource 

1 point
2.
Question 2
What is the relationship between parent and child elements in an HTML document tree used in BeautifulSoup?


Parent elements can only contain a maximum of three child elements 

Parent elements can only be accessed after all child elements are processed 

Child elements must always be of the same type as their parent elements 

Child elements are nested within parent elements, allowing navigation up and down the tree 

1 point
3.
Question 3
What is the relationship between JSON data and Python dictionaries when working with API responses?


JSON data requires special encoding to be accessed as a Python dictionary 

Python dictionaries can only store numerical values from JSON data 

JSON data must be converted to XML before it can be used in Python 

JSON data can be converted to Python dictionaries, maintaining the same key-value structure 

1 point
4.
Question 4
When working with time series data, what is the purpose of pandas’ to_datetime function?


To extract only the date portion from datetime objects 

To filter out non-business days from financial data 

To convert timestamps into standard datetime objects for easier analysis and visualization 

To compare dates across multiple datasets 

1 point
5.
Question 5
What is the role of the 'td' tag in HTML files?


Table row 

Table caption 

Table cell data 

Table description 

1 point
6.
Question 6
What is the structure of a query string in a GET request URL?


It begins with a question mark followed by parameter-value pairs separated by ampersands. 

It begins with a semicolon followed by JSON-formatted parameter values. 

It begins with an exclamation mark followed by XML-formatted data. 

It begins with a hash symbol followed by parameter names only. 

1 point
7.
Question 7
What distinguishes REST APIs from other types of APIs?


They communicate through the internet using HTTP methods and typically exchange data in formats like JSON. 

They require specialized hardware to function. 

They are restricted to local communication within a single application. 

They can only be implemented in Python. 

1 point
8.
Question 8
A data analyst has received three data files: customer_data.csv, inventory.json, and sales_report.xml. Which Python code would correctly read the CSV file?


import csv; csv.write('customer_data.csv') 

import json; with open('customer_data.csv') as f: data = json.load(f) 

import xml.etree.ElementTree as ET; tree = ET.parse('customer_data.csv') 

import pandas as pd; df = pd.read_csv('customer_data.csv') 

1 point
9.
Question 9
A developer is creating an application that needs to retrieve data from a REST API. Which code snippet correctly sends a GET request with parameters?


import requests; payload = {'name': 'John', 'ID': '123'}; r = requests.get('https://api.example.com/data', params=payload) 

import requests; r = requests.get('https://api.example.com/data?name=John&ID=123', data={'secure': True}) 

import requests; r = requests.post('https://api.example.com/data', headers={'name': 'John', 'ID': '123'}) 

import requests; r = requests.get('https://api.example.com/data', json={'name': 'John', 'ID': '123'}) 

1 point
10.
Question 10
How does a BeautifulSoup object internally represent an HTML document?


As a flat list of all HTML elements 

As a navigable tree structure where tags are nodes with parent-child relationships 

As a dictionary with tag names as keys and content as values 

As a series of SQL-like queries that extract content 